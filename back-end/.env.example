# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
# For production with authentication:
# MONGODB_URL=mongodb://username:password@host:27017/?authSource=admin

DATABASE_NAME=test_enrollment_db

# JWT Security - CRITICAL: Generate new secret for production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=REPLACE_WITH_GENERATED_SECRET_KEY_DO_NOT_USE_THIS_DEFAULT
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# CORS - Update for production domain
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
