# ============================================================================
# PRM Application Environment Configuration
# ============================================================================
# SECURITY WARNING: NEVER commit the actual .env file to version control!
# This .env.example file is for documentation purposes only.
# ============================================================================

# MongoDB Configuration
# Connection string for your MongoDB instance
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=test_enrollment_db

# ============================================================================
# JWT Authentication Configuration
# ============================================================================
# CRITICAL: Generate a cryptographically secure SECRET_KEY!
# Run this command to generate a strong key:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
#
# NEVER use simple passwords or the example below in production!
# ============================================================================
SECRET_KEY=REPLACE_THIS_WITH_SECURE_64CHAR_KEY_FROM_COMMAND_ABOVE
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120

# ============================================================================
# OpenAI API Configuration
# ============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# IMPORTANT: If you previously committed an API key to git, you MUST:
#   1. Revoke the old key immediately
#   2. Generate a new key
#   3. Never commit the actual key to version control
# ============================================================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo
ENABLE_REPORT_CACHING=true
REPORT_CACHE_DURATION=3600

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================
# Before deploying to production:
# [ ] Generate and set a strong SECRET_KEY (64+ characters)
# [ ] Rotate all API keys if any were previously exposed
# [ ] Use a secrets management service (AWS Secrets Manager, Azure Key Vault)
# [ ] Enable HTTPS/TLS for all connections
# [ ] Configure proper CORS origins
# [ ] Set up monitoring and alerting
# ============================================================================
